{
  vim.binds = {
    cheatsheet.enable = true;
    hardtime-nvim.enable = true;

    whichKey = {
      enable = true;
      setupOpts = {
        preset = "helix";
        icons = {
          separator = "│";
        };
      };
    };
  };
}
