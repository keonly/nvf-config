{
  vim.binds = {
    cheatsheet.enable = true;
    hardtime-nvim.enable = true;

    whichKey = {
      enable = true;
      setupOpts = {
        icons = {
          preset = "modern";
          separator = "󰁔";
        };
      };
    };
  };
}
